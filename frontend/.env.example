# Cognito (set these for production so the built app uses your user pool).
# Get values from Cognito → User pool → App clients.
# VITE_COGNITO_USER_POOL_ID=us-east-1_2tphwtJVz
# VITE_COGNITO_CLIENT_ID=72nacspbmmec7ghfurtr7dlom1

# Optional: Cognito Hosted UI domain for sign-out (from Cognito → Branding → Domain).
# VITE_COGNITO_DOMAIN=https://your-prefix.auth.us-east-1.amazoncognito.com

# Optional: fix redirect URI at build time (default is current origin at runtime).
# VITE_REDIRECT_URI=https://sentinelnetsolutions.com/
