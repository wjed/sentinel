# Optional: set for production deploy (e.g. CloudFront URL).
# Must match a URL in Cognito App client "Allowed callback URLs" and "Allowed sign-out URLs".
# VITE_REDIRECT_URI=https://d84l1y8p4kdic.cloudfront.net

# Optional: Cognito Hosted UI domain for sign-out redirect.
# From Cognito User Pool → App integration → Domain (e.g. https://your-prefix.auth.us-east-1.amazoncognito.com).
# VITE_COGNITO_DOMAIN=https://your-prefix.auth.us-east-1.amazoncognito.com
